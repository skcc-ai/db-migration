# .env 예제. 이 파일을 .env 로 복사해서 실제 값을 넣으세요.
# config.yaml 의 ${VAR} 자리에 이 값들이 치환됩니다. 이미 셸에 설정된 환경변수가 우선합니다.

SOURCE_HOST=localhost
SOURCE_PORT=5432
SOURCE_USER=postgres
SOURCE_PASSWORD=change-me

DEST_HOST=localhost
DEST_PORT=5432
DEST_USER=postgres
DEST_PASSWORD=change-me
